## 0. 핵심 동작 규칙 (최우선)

- 수다/설명 절대 금지 (코드 생성 불가 시에만 1줄 원인 출력)
- 출력 순서: **[변경 요약]** 1줄 → `// 파일명` → 결과 코드
- **파일 전체 출력 금지. diff 금지. 수정된 함수 단위 우선, 불가 시 클래스 단위**
- 계획/분석 선출력 금지. 바로 코드 출력.
- 리팩토링 금지: 요청 범위 외 구조 변경 금지
- `flutter run` 등 자동 실행 금지
- 동일 에러 2회 실패 시 재시도 금지 → 1줄 질문
- 요구사항 불명확 시 추측 구현 금지 → 1줄 질문
- 규칙 파일(CLAUDE.md, docs/rules) 수정 금지
- 테스트 코드 미요청 시 생성 금지
- 파일 여럿 수정 시: `// 파일명` → 수정 함수 → `---` 반복
- 코드 블록 외 자연어 출력은 [변경 요약] 1줄이 전부
- "완료했습니다" 등 단순 완료 멘트 금지. 완료 시 [개발완료] + 변경 요약 1줄 출력.
- 파일 탐색(Read/Grep) 결과 출력 금지. 코드에만 반영.
- 이전 대화 재요약 금지
- 변경 불필요 판단 시 1줄만 출력
- TODO 주석 미요청 시 생성 금지
- 대안 코드 미요청 시 출력 금지
- "추가 요청 있으면..." 등 대화 유도 멘트 금지

---

## 1. 사고 원칙

- 가정 금지. 불명확 시 즉시 질문.
- 해석이 여럿이면 제시 후 선택 요청.
- 더 단순한 방법이 있으면 먼저 제안.

---

## 2. 단순성

- 요청된 것만 구현. 추측 기능 금지.
- 단일 사용 코드에 추상화 금지.
- 200줄이 50줄로 가능하면 재작성.

---

## 3. 외과적 수정

- 요청된 부분만 수정. 인접 코드 개선 금지.
- 기존 스타일 유지 (개인 선호 반영 금지).
- 내 변경으로 생긴 orphan(미사용 import 등)만 제거.
- 기존 dead code 임의 삭제 절대 금지.

---

## 4. 목표 기반 실행

3단계 이상 작업: 시작 전 `docs/todo.md` **Overwrite** 1회.  
(작업 중 파일 업데이트 절대 금지)

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
```

---

## 5. Flutter+Firebase 개발 규칙

- 외부 백엔드 금지
- 언어/주석: 한글 필수. UI 주석 금지. DB/권한/상태 핵심 로직만 1줄 개조식.
- Enum 필수 (문자열 비교 금지)
- 위젯 `Semantics` 필수
- 폴더: `lib/core`, `lib/features` 유지 (임의 생성 금지)
- `flutter analyze` 통과 필수 (Warning 0개)
- 위젯 `const`, 불변 변수 `final` 필수
- `await` 후 `if (!mounted) return;` 필수
- `unused`, `dead_code` 임의 삭제 절대 금지
- 미요청 기능/필드/unused 코드 생성 금지
- null safety 준수
- UI 중첩 1 depth 최소화
- 사용 import만 포함
- `pubspec.yaml` 수정 금지

---

## 6. 백업

"요약 백업 해줘" 입력 시:  
→ 현재 상태/변수를 `docs/handover.md`에 **Overwrite** (누적 금지)  
→ 완료 후 딱 1줄: `[백업 완료] /clear 후 docs/handover.md 읽기`

---

## 7. 상세 규칙 위치

| 영역 | 경로 |
|------|------|
| core | docs/rules/core.md |
| UI | docs/rules/ui.md |
| 접근성 | docs/rules/accessibility.md |
| 백엔드 | docs/rules/backend.md |